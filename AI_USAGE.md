# AI Usage Note

> Fill this in based on what actually happened in your session — this is a
> starting draft, not a final answer. The assignment explicitly asks you to be
> able to explain the code you submit, so be specific and honest.

## Which AI tools I used
- Claude (Anthropic) — used to generate the initial full-stack scaffold
  (Next.js app structure, the extraction API route, the zod schema, and the
  chat UI) from a written specification of the assignment.
- `<add: Antigravity / Cursor / other tools you used to install, run, debug,
  or extend the scaffold>`

## What I used them for
- Turning the assignment brief into a concrete architecture (chat UI →
  extraction API → structured card) and a first working implementation.
- `<add: what you asked Antigravity or other tools to do — e.g. "install
  dependencies and fix a build error", "add the example-questions buttons",
  "deploy to Vercel">`

## What I personally designed
- `<add: e.g. "I chose to re-validate isComplete in code rather than trust
  the model's flag", "I decided against a database for this scope", "I chose
  Claude tool-use over parsing JSON from free text and can explain why">`

## What I reviewed or modified
- `<add: specific files/functions you read closely, tested, or changed —
  e.g. "I reviewed lib/llm.ts line by line and adjusted the system prompt
  after testing it asked two questions at once", "I tested the clarification
  loop with 5 different questions and fixed a bug where...">`
